import React from 'react'

const Button = ({style}) => {
  return (
    <button className={`py-4 px-6 bg-blue-gradient font-poppins ontline-none text-[18px] ${style} text-primary rounded-[10px]`}>
        GetStatrted
    </button>
  )
}

export default Button